import React, { Component } from "react";

// https://codesandbox.io/s/-w5szl old source for the draggable one, not really worth using but maybe you can get an idea from interval

class Dashboard extends Component {

    render() {
        return (
          <div></div>
        );
    }
}

// Put the thing into the DOM!
export default Dashboard
